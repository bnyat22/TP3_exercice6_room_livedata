<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.tp3_exercice6">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TP3_Exercice6">
        <activity android:name=".Inscription">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <activity android:name=".Profil"/>
        <activity android:name=".AllPlans"/>
        <activity android:name=".TodayPlans"/>
        <activity android:name=".MainPlanning"/>
        <activity android:name=".InsertPlan"/>
    </application>

</manifest>